import React from 'react';
import { connect } from 'react-redux';

const MobileChairSelector = props => (
  <div>
    BeepBoop ready to select.
  </div>
);

const mapStateToProps = state => ({

});

const mapDispatchToProps = state => ({

});

export default connect(mapStateToProps, mapDispatchToProps)(MobileChairSelector);
